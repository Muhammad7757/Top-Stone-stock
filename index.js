import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import GraniteStockManager from './GraniteStockManager';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <GraniteStockManager />
  </React.StrictMode>
);